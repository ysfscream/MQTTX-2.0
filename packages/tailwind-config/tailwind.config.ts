import type { Config } from 'tailwindcss'

const config: Config = {
  content: [],
  theme: {
    colors: {
      green: {
        primary: '#34c388',
      },
    },
  },
  plugins: [],
}
export default config
